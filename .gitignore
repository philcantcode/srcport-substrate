# Rust build output. With the root workspace, target/ lives at the repo root;
# the sdk/* entry covers standalone builds inside a crate dir.
/target/
/sdk/*/target/

# buf
buf.lock

# OS noise
.DS_Store
